# Paper inbox

Put source PDF files here before running the importer. PDFs and nested folders are ignored by Git, so copyrighted files remain local and are never published with the site.

Only files ending in `.pdf` are scanned. Photos, office documents, property records, and other file types are ignored automatically.

